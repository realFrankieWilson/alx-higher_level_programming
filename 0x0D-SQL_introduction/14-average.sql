-- Computes the score average of all records in the table second_table
-- of database hbtn_Oc_0 in Mysql server.
--	The result column name should be average
SELECT AVG(score) AS average FROM second_table;
